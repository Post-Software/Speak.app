[package]
name = "parakeet-worker"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
transcribe-rs = { path = "../vendor/transcribe-rs" }
